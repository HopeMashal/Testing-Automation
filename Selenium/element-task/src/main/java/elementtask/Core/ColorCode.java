package elementtask.Core;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class ColorCode {
  WebDriver driver;
	WebElement colorBox;
  WebElement colorText;

  public ColorCode(WebDriver Driver) {
    driver = Driver;
  }

  
}
